# 系统状态统一说明

**日期**: 2025-11-09  
**目的**: 统一整个系统中所有培训/课程状态的显示  
**状态**: 🔧 进行中  

---

## 📋 问题描述

系统中存在多处状态显示不一致的问题：

### 发现的不一致状态

| 位置 | 发现的状态文本 | 应统一为 |
|------|--------------|---------|
| 招商简章 - 关联课程 | 即将开展 | 即将开始 |
| 课程管理 | 进行中、期排中、计划中 | 使用统一状态 |
| 培训管理 | 即将开始、进行中、已完成 | ✅ 正确 |

### 状态不一致的影响

1. **用户困惑** - 同样的状态，不同地方显示不同的文字
2. **数据混乱** - 筛选和查询时可能出现问题
3. **维护困难** - 修改状态显示需要找到多处代码

---

## ✅ 统一方案

### 1. 创建统一的状态工具

**文件**: `src/utils/statusUtils.ts`

这个工具文件提供：
- 统一的状态文本映射
- 统一的状态颜色
- 状态计算函数
- 状态标准化函数

### 2. 标准状态定义

| 状态值 | 显示文本 | 颜色 | 说明 |
|--------|---------|------|------|
| `upcoming` | 即将开始 | 黄色 | 当前日期 < 开始日期 |
| `ongoing` | 进行中 | 蓝色 | 开始日期 ≤ 当前日期 ≤ 结束日期 |
| `completed` | 已完成 | 绿色 | 当前日期 > 结束日期 |
| `cancelled` | 已取消 | 灰色 | 手动取消的培训 |
| `planned` | 计划中 | 紫色 | 尚未确定日期的计划 |

### 3. 兼容旧数据

工具会自动映射不一致的状态：

```typescript
const mapping = {
  '即将开展': 'upcoming',  // 不一致 → 统一
  '期排中': 'ongoing',    // 不一致 → 统一
  '即将开始': 'upcoming',
  '进行中': 'ongoing',
  '已完成': 'completed'
};
```

---

## 🔧 使用方法

### 1. 导入工具函数

```typescript
import { getStatusText, getStatusClassName, getStatusBgColor } from '@/utils/statusUtils';
```

### 2. 显示状态文本

**修改前**：
```tsx
<span>
  {session.status === 'completed' ? '已完成' : 
   session.status === 'upcoming' ? '即将开始' : '进行中'}
</span>
```

**修改后**：
```tsx
<span>
  {getStatusText(session.status)}
</span>
```

### 3. 应用状态颜色

**修改前**：
```tsx
<span className={`px-2 py-1 text-xs rounded ${
  session.status === 'completed'
    ? 'bg-green-100 text-green-800'
    : session.status === 'upcoming'
    ? 'bg-yellow-100 text-yellow-800'
    : 'bg-blue-100 text-blue-800'
}`}>
```

**修改后**：
```tsx
<span className={`px-2 py-1 text-xs rounded ${getStatusClassName(session.status)}`}>
```

### 4. 完整示例

```tsx
<span className={`px-2 py-1 text-xs font-semibold rounded-full ${getStatusClassName(session.status)}`}>
  {getStatusText(session.status)}
</span>
```

---

## 📍 需要修改的位置

### 已修复 ✅
1. `src/pages/TrainingPerformance.tsx` - 培训计划管理
2. `src/components/TrainingSessionFormModal.tsx` - 课程场次表单

### 待修复 ⚠️

#### 1. 招商简章 - 关联课程模态框
**文件**: `src/pages/ProspectusManagement.tsx`  
**位置**: 第1688-1696行

```tsx
// 当前代码
<span className={`px-2 py-1 text-xs font-semibold rounded-full ${
  session.status === 'completed'
    ? 'bg-green-100 dark:bg-green-900/50 text-green-800 dark:text-green-300'
    : session.status === 'upcoming'
    ? 'bg-yellow-100 dark:bg-yellow-900/50 text-yellow-800 dark:text-yellow-300'
    : 'bg-blue-100 dark:bg-blue-900/50 text-blue-800 dark:text-blue-300'
}`}>
  {session.status === 'completed' ? '已完成' : session.status === 'upcoming' ? '即将开始' : '进行中'}
</span>

// 修改为
import { getStatusText, getStatusClassName } from '@/utils/statusUtils';

<span className={`px-2 py-1 text-xs font-semibold rounded-full ${getStatusClassName(session.status)}`}>
  {getStatusText(session.status)}
</span>
```

#### 2. 课程管理页面
**文件**: `src/pages/TrainingManagement.tsx`  
**位置**: 第177-184行

```tsx
// 当前代码
const getStatusText = (status: string | null) => {
  switch (status) {
    case 'planned': return '进行中';      // ❌ 错误
    case 'ongoing': return '期排中';      // ❌ 不一致
    case 'completed': return '已完成';
    case 'cancelled': return '已取消';
    default: return '未知';
  }
};

// 修改为
import { getStatusText, getStatusClassName } from '@/utils/statusUtils';
// 删除本地的 getStatusText 函数，使用统一工具
```

#### 3. 销售追踪页面
**文件**: `src/pages/SalesTracking.tsx`  
**位置**: 需要检查所有状态显示的地方

```tsx
// 导入统一工具
import { getStatusText, getStatusClassName } from '@/utils/statusUtils';

// 替换所有硬编码的状态显示
```

#### 4. 海报生成页面
**文件**: `src/pages/PosterGenerator.tsx`  
**位置**: 第355行

```tsx
// 当前代码
<p className="text-gray-500 text-sm">暂无即将开始的培训课程</p>

// 确保使用统一的状态判断
```

---

## 🎯 修复优先级

### P0 - 高优先级（影响用户体验）
1. ✅ **培训计划管理** - 已修复
2. ⚠️ **招商简章 - 关联课程** - 待修复（用户已反馈）
3. ⚠️ **课程管理** - 待修复（状态文本错误）

### P1 - 中优先级（数据一致性）
4. ⚠️ **销售追踪** - 待修复
5. ⚠️ **数据导出** - 待检查

### P2 - 低优先级（优化）
6. 所有筛选器使用统一的状态选项
7. 所有状态显示使用统一的颜色

---

## 🧪 测试检查清单

修改后需要验证：

- [ ] 培训计划列表 - 状态显示正确
- [ ] 培训详情页 - 状态显示正确
- [ ] 招商简章关联课程 - 状态显示正确且一致
- [ ] 课程管理列表 - 状态显示正确
- [ ] 销售追踪 - 状态显示正确
- [ ] 所有筛选器 - 状态选项一致
- [ ] 状态颜色 - 全局一致

---

## 📚 相关文档

- `docs/优化-培训状态自动计算.md` - 状态自动计算说明
- `docs/补充-课程管理表单状态优化.md` - 表单状态优化
- `src/utils/statusUtils.ts` - 统一状态工具代码

---

## ✅ 下一步行动

1. 修复 `ProspectusManagement.tsx` 中的状态显示
2. 修复 `TrainingManagement.tsx` 中的错误状态文本
3. 检查并修复 `SalesTracking.tsx`
4. 全面测试所有页面的状态显示
5. 更新文档

---

**状态**: 🟡 部分完成  
**工具**: ✅ 已创建  
**应用**: 🔧 进行中  

**目标：实现整个系统状态显示100%统一！** 🎯
